{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "echo 'No build step required for API-only project'"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "husky": "^8.0.3"
    }
}
